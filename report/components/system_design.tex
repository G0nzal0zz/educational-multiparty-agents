\section{System Design and Architecture}
\label{sec:design}

To implement the proposed multy-party dialog project, we designed and orchestrated a system with multiple different technologies. This section, gives an overview on the system design and technologies used.

\subsection{Architecture Overview}

As it can be seen in \autoref{fig:architecture}, the project is divided into 3 programs which work strategically together; Turn-taking Controller (TTC), Teacher and Student. The communication between the programs is based in a TCP socket connection where the TTC acts as the server and the Teacher and Student function as clients. The information which is transmitted through the sockets are serialized json objects containing events information, in \autoref{tab:socket-events} a list of all the possible events is shown and in \autoref{lst:json-event-example} an example of serialized JSON event can be seen.

\begin{table*}[htbp]
\small
\centering
\begin{tabular}{llll}
\toprule
\textbf{Event} & \textbf{Sender} & \textbf{Receiver} & \textbf{Description} \\ \midrule
\texttt{human\_transcription} & TTC & Teacher \& Student & Human speech transcription \\
\texttt{agent\_turn} & TTC & Teacher \& Student & Signal for agent to start turn \\
\texttt{agent\_turn\_cancelled} & TTC & Teacher \& Student & Turn cancelled (interrupted) \\
\texttt{agent\_text\_chunk} & Teacher \& Student & TTC & Streamed text chunk from agent stream \\
\texttt{agent\_text\_end} & Teacher \& Student & TTC & End of agent response \\
\bottomrule
\end{tabular}
\caption{Socket events between TTC server and clients}
\label{tab:socket-events}
\end{table*}

\begin{lstlisting}[
  language=json,
  caption={JSON agent\_text\_chunk event example},
  captionpos=b,
  label={lst:json-event-example},
]
{
    "type": "agent_text_chunk",
    "role": 2,
    "text": "Today's topic is the Spanish Empire.",
    "ts": 1713187200000
}
\end{lstlisting}

\subsubsection{TTC}
The TTC acts a the TCP server where the teacher and student establish connections to it. Furthermore, the TTC has 2 main roles. First, managing all the events happening in the system and taking decisions based on the status of the system. Second, handling all the information extracted from and trasmitted to the human. It is composed by 3 internal components:


\textbf{ASR.} It detects the user speech and streams the transcription of what the user said. When silence is detected the component emits an \texttt{end\_of\_utterance} event.

\textbf{TTS.} Works with streams of text, playing as audio what either agent generated.

\textbf{Event Handlers.} The main orchestrator of the whoel system. It received events from both agents, and from the \textbf{ASR} and \textbf{TTS}, based on that it manages all the system by selecting the next speaker or waiting for the human to talk if needed. based on that it manages all the system by selecting the next speaker or waiting for the human to talk if needed.



\subsubsection{Teacher}
The Teacher is a client program that connects to the TTC. It loads an initial lesson script and uses an LLM with a teaching-focused system prompt to generate responses to both students.

\subsubsection{Student}
The Student is a client program that connects to the TTC. It maintains conversation state and generates clarifying questions using an LLM focusing on improving tha human comprehension and knoweledge.

\end{multicols}
\begin{figure}[H]
  \centering
  \includegraphics[width=0.75\textwidth,height=0.65\textheight,keepaspectratio]{figures/system.png}
  \caption{Architecture of the proposed system. Boxes represent components, participants are presented as circles, and arrows indicate information flow from source to destination. Boxes with solid filling represent standalone programs, while those with hachured filling represent internal components of a program. Red lines denote events generated by components, while black lines represent raw data. Dashed lines indicate optional connections.}
  \label{fig:architectures}
\end{figure}
\begin{multicols}{2}

\subsection{Workflow}


This section will give an overview of the workflow of the system for both experiments: with peer student and without it.

% TODO: Mention all the possible events that can be sent through the sockets

\subsubsection{With peer student}
\label{sec:workflow_peer_student}

\autoref{fig:seq_combined} illustrates the system's sequence diagram. The interaction begins once both clients connect to the TTC, which sends an \texttt{agent\_turn} event to the teacher to initiate streaming of a pre-generated lesson. As text chunks arrive from the teacher, the TTC converts them to audio via TTS and plays them immediately. When the teacher finishes streaming, it emits an \texttt{agent\_text\_end} event to signal completion. The TTC relays this event to the Student, enabling it to prepare a potential response based on the Teacher's content.

After the audio playback concludes, the system allocates a 10-second response window. If the ASR detects and transcribes speech, a \texttt{human\_transcription} is sent to the Teacher. A brief acknowledgment audio is then played to allow the Teacher processing time. Conversely, if no speech is detected, the Student is prompted via an \texttt{agent\_turn} event to contribute its pre-generated text, which follows the same workflow as the initial Teacher input---including bouncing the \texttt{agent\_text\_end} event to the Teacher for response generation.

Regardless of which branch executes, the Teacher must formulate a response to either the human or the student's contribution. This is signaled by sending an \texttt{agent\_turn} event, which instructs the Teacher to speak.

\subsubsection{Without peer student}
\label{sec:workflow_without_peer_student}
\autoref{fig:seq_teacher} illustrates the simplified sequence diagram for the teacher-student interaction. The workflow mirrors \autoref{sec:workflow_peer_student} with two key differences:

\begin{enumerate}[noitemsep, leftmargin=*]
  \item The \texttt{agent\_text\_end} event is sent directly from the Teacher to the TTC without relaying to a Student agent, as there is no peer student in the system.
  \item If no speech is detected during the 10-second response window, the Teacher automatically receives an \texttt{agent\_turn} event to continue elaborating on the taught topic, rather than waiting for a student agent response.
\end{enumerate}

The core interaction loop remains identical: the Teacher streams text chunks, the TTC converts them to audio via TTS and plays them, and the human has an opportunity to respond. However, without the Student's pre-generated input branch, the system either processes the human's transcribed speech or prompts the Teacher to continue the lesson.

\subsection{Technology Stack}

This system leverages three core AI models to enable natural voice-based interaction: Automatic Speech Recognition (ASR), Text-to-Speech (TTS), and a Large Language Model (LLM).

\subsubsection{ASR}
\textbf{Whisper} (OpenAI) handles speech-to-text conversion. The system uses the \texttt{openai-whisper} library to transcribe microphone input in real-time. We use the \texttt{small} model (244M parameters). Since Whisper does not natively support streaming, we implemented a custom async pipeline that accumulates audio chunks, transcribes them incrementally, and uses silence detection to emit end-of-utterance events.
\subsubsection{TTS}
\textbf{Chatterbox-Turbo} (ResembleAI) converts agent text back to speech. This model supports audio prompt conditioning, allowing voice cloning from sample recordings. Since Chatterbox-Turbo does not natively support streaming, audio chunks are queued and played sequentially via \texttt{sounddevice} to achieve pseudo-streaming playback.

\subsubsection{LLM}
\textbf{Llama 3.2} (via Ollama) powers the conversational AI. The model is accessed through LangChain's \texttt{ChatOllama} interface with streaming enabled. Responses are yielded incrementally as full sentences are completed. The teacher and student clients each run independent instances of Llama 3.2, enabling separate conversational flows and configurable system prompts for each role.

